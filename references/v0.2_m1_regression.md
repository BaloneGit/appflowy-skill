# v0.2 M1 真实联调记录（2026-03-02）

## 环境

- AppFlowy base URL: `http://10.60.0.189`
- client version: `0.12.3`
- workspace: `80a763b4-fa42-43a2-a3e1-efa2ac9c62ac`
- database: `41fb4313-6d9e-474c-b58f-69e879148be5`

## 命令回归结果

1. `database-query` 基础分页
   - 命令：`python skills/appflowy-api/scripts/appflowy_skill.py database-query ... --limit 3`
   - 结果：成功，`rows_loaded=6`，`rows_returned=3`
2. `database-query` 快速过滤+排序
   - 命令：`... --filter-field "Status-SqwRg1" --filter-op eq --filter-value "To Do" --sort-field Description --sort-direction asc --limit 10`
   - 结果：成功，`rows_after_filter=3`
3. `database-query` query-file（UTF-8 BOM）
   - 命令：`... --query-file .tmp/db_query_case1.json`
   - 结果：成功，验证 `--query-file` 可读取 UTF-8 BOM
4. `page-get-tree` 工作区树
   - 命令：`python skills/appflowy-api/scripts/appflowy_skill.py page-get-tree ... --depth 3 --compact`
   - 结果：成功，返回 workspace 下页面树
5. `page-get-tree` 子树
   - 命令：`... --root-view-id cb089dda-3a69-4c99-8629-64418398dbbe --depth 1 --compact`
   - 结果：成功，返回 General 子树
6. `page-get-blocks` 树形输出
   - 命令：`python skills/appflowy-api/scripts/appflowy_skill.py page-get-blocks ... --view-id 34987a3a-0c2d-4777-a755-c02d3caa4c1d`
   - 结果：成功，`block_count=16`，`root_block_count=14`
7. `page-get-blocks` 扁平输出
   - 命令：`... --flat`
   - 结果：成功，返回 `level/type/text_preview` 扁平列表

## 发现与修复

1. `page-get-blocks` 初版根节点提取错误（`children_map[page_id]`），导致返回空树。
   - 修复：改为优先读取 `blocks[page_id].children -> children_map[children_key]`，并保留旧结构兜底。
2. `database-query --query-file` 在 Windows 下读取 UTF-8 BOM 会报 `JSONDecodeError`。
   - 修复：`_common.py` 中 `load_config/load_json_payload` 改为 `utf-8-sig` 读取。
