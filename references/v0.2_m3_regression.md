# v0.2 M3 真实联调记录（2026-03-02）

## 环境

- AppFlowy base URL: `http://10.60.0.189`
- client version: `0.12.3`
- workspace: `80a763b4-fa42-43a2-a3e1-efa2ac9c62ac`
- database: `41fb4313-6d9e-474c-b58f-69e879148be5`

## 新增能力联调

1. `bulk-upsert-rows` dry-run
   - 输入：`.tmp/m3_bulk_rows_1.json`（2 行）
   - 结果：`planned_count=2`，`skipped_count=2`，输出 `change_report.before/plan/after/summary`
2. `bulk-upsert-rows` 首次执行（新增）
   - 结果：`added_count=2`，`updated_count=0`，`row_count_diff=+2`
3. `bulk-upsert-rows` 二次执行（更新）
   - 输入：`.tmp/m3_bulk_rows_2.json`（同 pre_hash，不同 cells）
   - 结果：`added_count=0`，`updated_count=2`，`row_count_diff=0`

## 删除类 dry-run 协议联调

1. `delete-rows --dry-run`
   - 结果：返回 `planned_delete_count`，不落库；`change_report.after.row_count_after` 与执行前一致
2. `delete-rows` 执行
   - 删除测试行：`6ba33f01-ab20-5d5d-e866-052afcd31d83`、`a0d9b95c-da7a-61be-3fdf-b039e660ab2c`
   - 结果：`deleted_count=2`，`row_count_after` 从 8 回到 6
3. `page-delete-blocks --dry-run`
   - 结果：返回 `planned_delete_block_ids` 与 `planned_delete_count`
4. `delete-db-field` dry-run
   - 结果：返回目标字段在各 view 的引用摘要 + `change_report`

## 协议检查

1. M3 统一输出协议已生效：`before` / `plan` / `after` / `summary`
2. 批量写入支持结果分类：`added` / `updated` / `failed`
3. `delete_rows.py`、`delete_page_blocks.py`、`delete_db_field.py` 已统一接入 `change_report`

## 清理说明

1. M3 批量写入联调创建的测试行已删除，数据库行数恢复到联调前水平（6 行）。
