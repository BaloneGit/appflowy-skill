# v0.2 M2 真实联调记录（2026-03-02）

## 环境

- AppFlowy base URL: `http://10.60.0.189`
- client version: `0.12.3`
- workspace: `80a763b4-fa42-43a2-a3e1-efa2ac9c62ac`
- database: `41fb4313-6d9e-474c-b58f-69e879148be5`

## 命令回归结果

1. `page-delete-blocks` dry-run
   - 测试页：`99163a0d-dfc6-48b8-9ea4-818af7595c5c`
   - 删除目标：`kXrIhkXcDO`
   - 结果：成功，返回 `delete_count=1`
2. `page-delete-blocks` 执行删除
   - 删除前：`block_count=4`
   - 删除后：`block_count=3`
   - 结果：成功，目标 block 已从页面树消失
3. `rename-db-field` dry-run
   - 新建测试字段：`RqIAxO` / `codex_m2_temp_field`
   - 结果：成功，预览 `old_name -> new_name`
4. `rename-db-field` 执行改名
   - 目标字段：`RqIAxO`
   - 结果：成功，`verified_name=codex_m2_temp_field_renamed`
5. `delete-db-field` dry-run
   - 目标字段：`RqIAxO`
   - 结果：成功，输出所有 view 中的 `field_orders/field_settings` 引用摘要
6. `delete-db-field` 执行删除
   - 命令要求：`--execute --yes`
   - 结果：成功，`deleted_field_ids=["RqIAxO"]`
7. 字段删除结果复核
   - REST fields：字段不存在
   - DB collab `fields`：字段不存在
   - 各 view `field_orders/field_settings`：字段引用已清理
8. 安全护栏校验
   - 删除主字段 `phVRgL`：按预期失败，提示 `Primary field deletion is not allowed`
   - 用 `--field-name Status` 改名：按预期失败，提示字段名重复，要求改用 `field_id`

## 发现与修复

1. 字段改名/删除没有稳定的 REST API。
   - 处理：新增 collab 脚本 `collab_rename_fields.mjs`、`collab_delete_fields.mjs`
2. 字段删除需要同步清理 view 引用。
   - 处理：删除时同步移除 `field_orders`、`field_settings`、`filters`、`sorts`、`groups`
3. 页面 block 删除需要可预演。
   - 处理：`delete_page_blocks.py` 新增 `--dry-run`，输出根 block 与递归删除集摘要
