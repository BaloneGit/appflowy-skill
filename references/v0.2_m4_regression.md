# v0.2 M4 真实回归记录（2026-03-03）

## 环境

- AppFlowy base URL: `http://10.60.0.189`
- client version: `0.12.3`
- workspace: `80a763b4-fa42-43a2-a3e1-efa2ac9c62ac`
- database: `41fb4313-6d9e-474c-b58f-69e879148be5`
- 回归清单：`skills/appflowy-api/references/v0.2_regression_suite.md`

## 回归执行结果

1. 查询与读取
- `database-query --limit 2`：通过，`rows_loaded=6`，`rows_returned=2`
- `database-query` filter+sort：通过，`rows_after_filter=3`
- `page-get-tree --depth 1 --compact`：通过
- `page-get-blocks --flat`：通过，`block_count=16`

2. 删除与字段（M2）
- `page-delete-blocks --dry-run`：通过，输出 `planned_delete_count=1`
- `page-delete-blocks` 执行：通过，删除测试 block 后 `block_count 4 -> 3`
- `rename-db-field --dry-run`：通过（不落库）
- `delete-db-field --dry-run`：通过，输出字段引用摘要与 `change_report`

3. dry-run + diff + 批量写入（M3）
- `delete-rows --dry-run`：通过，`change_report` 存在且不落库
- `bulk-upsert-rows --dry-run`：通过，`planned_count=2`，`skipped_count=2`
- `bulk-upsert-rows` 首次执行：通过，`added_count=2`
- `bulk-upsert-rows` 二次执行：通过，`updated_count=2`
- `delete-rows` 清理测试行：通过，`deleted_count=2`，行数恢复到 6

4. 发布目录 smoke
- `release/.../appflowy_skill.py list`：通过
- `release` 新命令 `help bulk-upsert-rows`：通过
- `release` dry-run 命令输出 `change_report`：通过

## 结论

1. v0.2 的 M1~M3 能力在真实环境回归通过。
2. 统一 dry-run 与 `change_report` 输出在删除类与批量写入类命令中稳定可用。
3. 发布目录与源目录功能一致，满足 v0.2 发布前验收条件。
