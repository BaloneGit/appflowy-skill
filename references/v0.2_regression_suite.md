# v0.2 回归脚本清单（M4）

## 测试环境

- base url: `http://10.60.0.189`
- workspace: `80a763b4-fa42-43a2-a3e1-efa2ac9c62ac`
- database: `41fb4313-6d9e-474c-b58f-69e879148be5`

## 回归项

1. 查询与读取能力
- [x] `database-query`：基础分页
- [x] `database-query`：filter + sort
- [x] `page-get-tree`：workspace 树
- [x] `page-get-blocks`：树形与 `--flat`

2. 删除与字段能力（M2）
- [x] `page-delete-blocks --dry-run`
- [x] `page-delete-blocks` 执行删除并验证
- [x] `rename-db-field --dry-run`
- [x] `delete-db-field --dry-run`

3. dry-run + diff + 批量写入（M3）
- [x] `delete-rows --dry-run`
- [x] `bulk-upsert-rows --dry-run`
- [x] `bulk-upsert-rows` 首次执行（新增）
- [x] `bulk-upsert-rows` 再次执行（更新）
- [x] `delete-rows` 清理测试行

4. 发布目录 smoke
- [x] `release/appflowy-api-skill/scripts/appflowy_skill.py list`
- [x] `release` 中新命令 `--help`

## 通过标准

1. 所有命令退出码为 0。
2. dry-run 命令不落库，且输出包含 `change_report`。
3. 写命令结果与摘要一致（新增/更新/删除计数可核对）。
4. 发布目录 smoke 全部通过。
